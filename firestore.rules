rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // This rule allows read and write access to all documents in all collections
    // for any user, authenticated or not.
    // WARNING: This is insecure and should only be used for temporary debugging.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}